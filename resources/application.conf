ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [
            community.flock.ApplicationKt.main,
            community.flock.jedi.JediModuleKt.module,
            community.flock.sith.SithModuleKt.module,
        ]
    }
    db {
        host = "localhost"
        host = ${?DB_HOST}
    }
}
